import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        background: {
            default: "#e5e5e5",
            paper: "#fff",
        },
    },
});

export default theme;
